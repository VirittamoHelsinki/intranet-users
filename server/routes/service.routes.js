import express from 'express';

const service = express.Router();

service.all("*")
service.all("*");
service.get("/public");
service.get("/");
service.post("/");
service.delete("/:id");
service.put("/:id");
